---
import NavMenu from "../components/NavMenu.astro";
const { title } = Astro.props;
---

<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
        <script src="./js/phaser.min.js" type="module"></script>
    </head>
    <body>
        <main class="main">
            <NavMenu />
            <h1 class="game__h1">Sobre mí</h1>
            <div class="game" id="game"></div>
        </main>
        <script src="./js/game.js" type="module"></script>
    </body>
</html>
<style is:global>
    @font-face {
        font-family: "Norse";
        src: url("/fonts/Norse.otf");
    }
    @font-face {
        font-family: "Poppins";
        src: url("/fonts/Poppins-Light.ttf") format("truetype");
    }
    :root {
        /* 🎨 Colores principales */
        --color-background: #0f172a; /* Fondo principal (dark navy) */
        --color-surface: #1e293b; /* Fondo de secciones / tarjetas */
        --color-surface-hover: #334155; /* Hover para tarjetas / botones */
        --color-border: #334155; /* Bordes / divisores */
        --color-shadow: rgba(0, 0, 0, 0.6); /* Sombras suaves */

        --color-text-primary: #f8fafc; /* Texto principal (blanco puro) */
        --color-text-secondary: #94a3b8; /* Texto secundario (gris azulado) */
        --color-text-muted: #64748b; /* Texto más sutil o inactivo */

        /* 🎯 Colores de marca */
        --color-primary: #3b82f6; /* Azul principal (botones, enlaces) */
        --color-primary-shadow: rgba(
            59,
            130,
            246,
            0.6
        ); /* Azul principal (botones, enlaces) */
        --color-primary-hover: #2563eb; /* Hover azul más oscuro */
        --color-primary-active: #1d4ed8; /* Estado activo / pulsado */

        /* 🌿 Colores de acento */
        --color-accent-green: #10b981; /* Verde menta (éxito, CTA secundaria) */
        --color-accent-green-hover: #059669;
        --color-accent-purple: #8b5cf6; /* Púrpura suave (para detalles creativos) */
        --color-accent-purple-hover: #a78bfa;

        /* 🪄 Animaciones y transiciones */
        --transition-fast: 0.15s ease-in-out;
        --transition-medium: 0.3s ease-in-out;
        --transition-slow: 0.6s ease-in-out;
        --animation-fade-in: fadeIn 0.6s ease-out forwards;
        --animation-slide-up: slideUp 0.5s ease-out forwards;

        /* 🪶 Radio y spacing */
        --radius-small: 0.5rem;
        --radius-medium: 1rem;
        --radius-large: 1.5rem;

        --spacing-xs: 0.5rem;
        --spacing-sm: 1rem;
        --spacing-md: 2rem;
        --spacing-lg: 4rem;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    html {
        color-scheme: dark light;
        scroll-behavior: smooth;
        background-color: var(--color-background);
        color: white;
        font-family: "Poppins";
    }
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: "Norse";
    }
    .main {
        background: radial-gradient(
                circle at 10% 40%,
                rgb(139, 92, 246, 0.4) 0%,
                /* Color del foco izquierdo (morado con transparencia) */
                    transparent 50% /* Se desvanece a transparente */
            ),
            radial-gradient(
                circle at 80% 90%,
                rgb(60, 131, 246, 0.3) 0%,
                /* Color del foco inferior derecho (morado más oscuro con transparencia) */
                    transparent 50% /* Se desvanece a transparente */
            ),
            var(--color-background); /* Color de fondo base (morado muy oscuro) */
        color: var(--color-text-primary);
        overflow-x: hidden;
        min-height: 100vh;
        min-width: 100vw;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        gap: 32px;
    }
</style>
